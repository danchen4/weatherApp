<!DOCTYPE html>
<html lang="en">

{{>head}}

<body>

  {{>header}}
  <div class="main-content">
    <div class="container">
      <h1>{{title}}</h1>
      <p>
        {{message}}
      </p>
    </div>
  </div>
  {{>footer}}
</body>

</html>